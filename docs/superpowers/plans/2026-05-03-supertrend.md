# supertrend R Package Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a CRAN-quality R package providing the SuperTrend technical indicator, signal extraction, and a quantmod-idiomatic chart overlay.

**Architecture:** Four exported functions (`SuperTrend`, `supertrend_signals`, `addSuperTrend`, `chartSuperTrend`) backed by `TTR::ATR()` for ATR computation. Standard R-package layout with roxygen2 docs, testthat tests, one vignette, and a built-in synthetic dataset for examples. Recursive band/trend loop in plain R (not vectorizable).

**Tech Stack:** R (≥ 3.6.0), `xts`, `zoo`, `TTR`, `quantmod`. Tests with `testthat` (edition 3). Docs via `roxygen2`. Vignette via `knitr` + `rmarkdown`.

**Spec:** `docs/superpowers/specs/2026-05-03-supertrend-design.md`

---

## File Structure

| Path | Responsibility |
|---|---|
| `DESCRIPTION` | Package metadata, dependencies, license |
| `NAMESPACE` | Auto-generated by roxygen2 from `@export` / `@importFrom` tags |
| `.Rbuildignore` | Exclude dev-only files from the built tarball |
| `.gitignore` | (already created) |
| `R/package.R` | Package-level docs + `@importFrom` declarations |
| `R/SuperTrend.R` | `SuperTrend()` core calculation |
| `R/signals.R` | `supertrend_signals()` |
| `R/addSuperTrend.R` | `addSuperTrend()` quantmod overlay |
| `R/chartSuperTrend.R` | `chartSuperTrend()` convenience wrapper |
| `R/data.R` | Roxygen documentation for the built-in dataset |
| `data/spy_sample.rda` | Built-in synthetic OHLC dataset (≈252 daily bars) |
| `data-raw/build_spy_sample.R` | Script that generates `spy_sample.rda` (not built into the package) |
| `tests/testthat.R` | testthat entry point |
| `tests/testthat/helper-data.R` | Synthetic OHLC fixture for unit tests |
| `tests/testthat/test-supertrend.R` | Tests for `SuperTrend()` |
| `tests/testthat/test-signals.R` | Tests for `supertrend_signals()` |
| `tests/testthat/test-addSuperTrend.R` | Tests for `addSuperTrend()` / `chartSuperTrend()` |
| `vignettes/supertrend.Rmd` | Walkthrough vignette |
| `README.md` | Top-level readme |

---

## Task 1: Bootstrap the package skeleton

**Files:**
- Create: `DESCRIPTION`
- Create: `.Rbuildignore`
- Create: `R/package.R`
- Create: `NAMESPACE` (placeholder; will be regenerated by roxygen2)
- Create: `tests/testthat.R`

- [ ] **Step 1: Write DESCRIPTION**

Create `DESCRIPTION`:

```
Package: supertrend
Type: Package
Title: SuperTrend Indicator and Signals for 'quantmod'
Version: 0.1.0
Authors@R: person(given = "Lars", family = "Bernhardsson",
                  email = "cran.sherry228@passinbox.com",
                  role = c("aut", "cre"))
Description: Computes the SuperTrend technical indicator (Olivier Seban,
    based on Average True Range) and extracts long/short entry and exit
    signals from trend flips. Provides a 'quantmod'-idiomatic chart
    overlay so the indicator can be added to existing 'chartSeries' plots
    in the same way as 'addBBands' or 'addMACD'. The SuperTrend
    calculation matches the conventions used by major charting platforms
    such as TradingView.
License: MIT + file LICENSE
URL: https://github.com/your-account/supertrend
BugReports: https://github.com/your-account/supertrend/issues
Depends:
    R (>= 3.6.0)
Imports:
    xts,
    zoo,
    TTR,
    quantmod
Suggests:
    testthat (>= 3.0.0),
    knitr,
    rmarkdown
Encoding: UTF-8
LazyData: true
VignetteBuilder: knitr
Config/testthat/edition: 3
```

Also create the LICENSE file required by `MIT + file LICENSE`:

`LICENSE`:
```
YEAR: 2026
COPYRIGHT HOLDER: Lars Bernhardsson
```

- [ ] **Step 2: Write .Rbuildignore**

Create `.Rbuildignore`:

```
^.*\.Rproj$
^\.Rproj\.user$
^data-raw$
^docs$
^README\.Rmd$
^\.github$
^\.gitignore$
```

- [ ] **Step 3: Write R/package.R with import declarations**

Create `R/package.R`:

```r
#' supertrend: SuperTrend Indicator and Signals for 'quantmod'
#'
#' Computes the SuperTrend technical indicator and extracts trading
#' signals from trend flips. Provides a 'quantmod'-idiomatic chart
#' overlay.
#'
#' @keywords internal
#' @importFrom xts xts is.xts
#' @importFrom zoo index coredata
#' @importFrom TTR ATR
#' @importFrom quantmod has.HLC Hi Lo Cl chartSeries addTA
"_PACKAGE"
```

- [ ] **Step 4: Write a placeholder NAMESPACE**

Create `NAMESPACE` (will be overwritten by `devtools::document()` once any function exists):

```
# Generated by roxygen2: do not edit by hand
```

- [ ] **Step 5: Write tests/testthat.R**

Create `tests/testthat.R`:

```r
library(testthat)
library(supertrend)

test_check("supertrend")
```

- [ ] **Step 6: Verify the skeleton parses**

Run from the package root:

```bash
R -q -e 'devtools::document()'
```

Expected: regenerates `NAMESPACE` and creates `man/supertrend-package.Rd`. No errors.

If `devtools` is not installed:

```bash
R -q -e 'install.packages(c("devtools", "roxygen2"), repos = "https://cloud.r-project.org")'
```

- [ ] **Step 7: Commit**

```bash
git add DESCRIPTION LICENSE .Rbuildignore R/package.R NAMESPACE tests/testthat.R man/
git commit -m "Bootstrap supertrend package skeleton"
```

---

## Task 2: Add the built-in sample dataset

**Files:**
- Create: `data-raw/build_spy_sample.R`
- Create: `data/spy_sample.rda`
- Create: `R/data.R`

- [ ] **Step 1: Write the data-generation script**

Create `data-raw/build_spy_sample.R`:

```r
# Generates data/spy_sample.rda — a deterministic synthetic OHLC series
# resembling daily SPY data. No network access required.
# Re-run this manually after editing; not run during package build.

set.seed(20260503)
n <- 252  # one trading year
dates <- seq(as.Date("2025-01-02"), by = "day", length.out = n)

log_returns <- stats::rnorm(n, mean = 0.0005, sd = 0.012)
close <- 470 * exp(cumsum(log_returns))

intraday_range <- abs(stats::rnorm(n, mean = 0.008, sd = 0.004)) * close
high <- close + stats::runif(n, 0, 1) * intraday_range
low  <- close - stats::runif(n, 0, 1) * intraday_range
open <- low + stats::runif(n, 0, 1) * (high - low)

# Enforce OHLC consistency: high >= max(O,C), low <= min(O,C)
hi <- pmax(open, high, low, close)
lo <- pmin(open, high, low, close)

mat <- cbind(Open = open, High = hi, Low = lo, Close = close)
spy_sample <- xts::xts(mat, order.by = dates)

dir.create("data", showWarnings = FALSE)
save(spy_sample, file = "data/spy_sample.rda",
     version = 2, compress = "xz")
```

- [ ] **Step 2: Run the script to generate data/spy_sample.rda**

```bash
R -q -e 'source("data-raw/build_spy_sample.R")'
```

Expected: creates `data/spy_sample.rda` (~10 KB).

Verify:

```bash
R -q -e 'load("data/spy_sample.rda"); print(head(spy_sample, 3)); print(dim(spy_sample))'
```

Expected: an xts with 252 rows, 4 columns (`Open`, `High`, `Low`, `Close`).

- [ ] **Step 3: Document the dataset**

Create `R/data.R`:

```r
#' Synthetic daily OHLC sample data
#'
#' A deterministic synthetic OHLC series resembling one trading year of
#' SPY-style daily bars. Generated locally with a fixed seed so examples,
#' tests, and the vignette work without network access.
#'
#' @format An \code{xts} object with 252 rows and 4 columns:
#' \describe{
#'   \item{Open}{Opening price.}
#'   \item{High}{High of the bar.}
#'   \item{Low}{Low of the bar.}
#'   \item{Close}{Closing price.}
#' }
#' @source Synthetic; generated by \code{data-raw/build_spy_sample.R}
#'   in the package source.
"spy_sample"
```

- [ ] **Step 4: Regenerate documentation**

```bash
R -q -e 'devtools::document()'
```

Expected: creates `man/spy_sample.Rd`. No errors.

- [ ] **Step 5: Commit**

```bash
git add data-raw/ data/ R/data.R man/spy_sample.Rd NAMESPACE
git commit -m "Add built-in synthetic OHLC sample dataset"
```

---

## Task 3: Write the test fixture helper

**Files:**
- Create: `tests/testthat/helper-data.R`

This helper builds a small synthetic OHLC xts used by `test-supertrend.R` and `test-signals.R`. It also exposes property-based fixtures (monotonic up, monotonic down) used to assert algorithmic invariants.

- [ ] **Step 1: Write the helper**

Create `tests/testthat/helper-data.R`:

```r
# Test fixtures for SuperTrend tests.
# All series are deterministic; no randomness, no I/O.

# 30-bar OHLC where price drifts up then sharply reverses, producing
# at least one trend flip with the default n=10, multiplier=3 settings.
make_mixed_hlc <- function() {
  n <- 30
  # First 20 bars: gentle uptrend. Last 10: sharp downtrend.
  close <- c(
    seq(100, 119, length.out = 20),
    seq(118, 95,  length.out = 10)
  )
  high <- close + 0.5
  low  <- close - 0.5
  dates <- as.Date("2025-01-01") + seq_len(n) - 1L
  xts::xts(cbind(High = high, Low = low, Close = close), order.by = dates)
}

# Strictly monotonic increasing closes — trend should be +1 after warm-up.
make_monotonic_up_hlc <- function(n = 40) {
  close <- seq(100, 100 + n - 1, length.out = n)
  high <- close + 0.5
  low  <- close - 0.5
  dates <- as.Date("2025-01-01") + seq_len(n) - 1L
  xts::xts(cbind(High = high, Low = low, Close = close), order.by = dates)
}

# Strictly monotonic decreasing closes — trend should be -1 after warm-up
# (and after the initial flip from the +1 seed).
make_monotonic_down_hlc <- function(n = 40) {
  close <- seq(200, 200 - (n - 1), length.out = n)
  high <- close + 0.5
  low  <- close - 0.5
  dates <- as.Date("2025-01-01") + seq_len(n) - 1L
  xts::xts(cbind(High = high, Low = low, Close = close), order.by = dates)
}
```

- [ ] **Step 2: Commit**

```bash
git add tests/testthat/helper-data.R
git commit -m "Add test fixture helpers for SuperTrend"
```

---

## Task 4: Implement SuperTrend() — structural tests + initial implementation

**Files:**
- Create: `tests/testthat/test-supertrend.R`
- Create: `R/SuperTrend.R`

- [ ] **Step 1: Write the failing structural tests**

Create `tests/testthat/test-supertrend.R`:

```r
test_that("SuperTrend returns xts with the expected columns and length", {
  hlc <- make_mixed_hlc()
  out <- SuperTrend(hlc, n = 10, multiplier = 3)

  expect_s3_class(out, "xts")
  expect_equal(colnames(out),
               c("supertrend", "trend", "upper_band", "lower_band"))
  expect_equal(nrow(out), nrow(hlc))
  expect_equal(zoo::index(out), zoo::index(hlc))
})

test_that("SuperTrend warm-up rows are NA", {
  hlc <- make_mixed_hlc()
  n <- 10
  out <- SuperTrend(hlc, n = n, multiplier = 3)

  # First n rows have NA ATR -> NA SuperTrend
  expect_true(all(is.na(out$supertrend[1:n])))
  expect_true(all(is.na(out$trend[1:n])))

  # Beyond warm-up, no NAs
  expect_false(any(is.na(out$supertrend[(n + 1):nrow(out)])))
  expect_false(any(is.na(out$trend[(n + 1):nrow(out)])))
})

test_that("SuperTrend trend column only contains -1, +1, or NA", {
  hlc <- make_mixed_hlc()
  out <- SuperTrend(hlc, n = 10, multiplier = 3)
  vals <- unique(as.numeric(out$trend))
  expect_true(all(vals %in% c(-1, 1, NA)))
})

test_that("SuperTrend on a strictly rising series stays in uptrend", {
  hlc <- make_monotonic_up_hlc()
  out <- SuperTrend(hlc, n = 10, multiplier = 3)
  trend_after_warmup <- as.numeric(out$trend[11:nrow(out)])
  expect_true(all(trend_after_warmup == 1))
})

test_that("SuperTrend on a strictly falling series flips to downtrend and stays", {
  hlc <- make_monotonic_down_hlc()
  out <- SuperTrend(hlc, n = 10, multiplier = 3)
  # After enough bars for the initial flip, trend must be -1.
  # Conservative: check the last 20 bars.
  tail_trend <- as.numeric(out$trend[(nrow(out) - 19):nrow(out)])
  expect_true(all(tail_trend == -1))
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
R -q -e 'devtools::test(filter = "supertrend")'
```

Expected: all 5 tests fail with `could not find function "SuperTrend"` (or similar).

- [ ] **Step 3: Write the implementation**

Create `R/SuperTrend.R`:

```r
#' SuperTrend Indicator
#'
#' Computes the SuperTrend technical indicator from an OHLC \code{xts}
#' series. The calculation matches the convention used by TradingView's
#' \code{ta.supertrend}: upper and lower bands derived from the median
#' price plus or minus a multiple of the Average True Range (ATR), with
#' bands trailing in the direction of the trend until price closes
#' through them.
#'
#' @param HLC An \code{xts} object containing High, Low, and Close
#'   columns. Validated with \code{\link[quantmod]{has.HLC}}.
#' @param n Integer. ATR period. Defaults to 10 (TradingView default).
#' @param multiplier Numeric. ATR band multiplier. Defaults to 3
#'   (TradingView default).
#' @param atr_method Character. ATR smoothing method. One of
#'   \code{"wilder"} (default, matches TradingView and most charting
#'   platforms), \code{"sma"}, or \code{"ema"}.
#'
#' @return An \code{xts} aligned to the input index with four columns:
#' \describe{
#'   \item{supertrend}{The SuperTrend line value.}
#'   \item{trend}{Trend direction: \code{+1} (uptrend) or \code{-1}
#'     (downtrend).}
#'   \item{upper_band}{Final (trailing) upper band.}
#'   \item{lower_band}{Final (trailing) lower band.}
#' }
#' The first \code{n} rows are \code{NA} (ATR warm-up).
#'
#' @examples
#' data(spy_sample)
#' st <- SuperTrend(spy_sample, n = 10, multiplier = 3)
#' head(st, 15)
#'
#' @export
SuperTrend <- function(HLC, n = 10, multiplier = 3,
                       atr_method = c("wilder", "sma", "ema")) {
  atr_method <- match.arg(atr_method)

  if (!xts::is.xts(HLC)) {
    stop("HLC must be an xts object")
  }
  if (!all(quantmod::has.HLC(HLC))) {
    stop("HLC must contain High, Low, and Close columns")
  }
  if (!is.numeric(n) || length(n) != 1L || is.na(n) || n < 1 ||
      n != as.integer(n)) {
    stop("n must be a positive integer")
  }
  if (!is.numeric(multiplier) || length(multiplier) != 1L ||
      is.na(multiplier) || multiplier <= 0) {
    stop("multiplier must be positive")
  }

  hi <- as.numeric(quantmod::Hi(HLC))
  lo <- as.numeric(quantmod::Lo(HLC))
  cl <- as.numeric(quantmod::Cl(HLC))
  N <- length(cl)

  atr_xts <- switch(
    atr_method,
    wilder = TTR::ATR(HLC, n = n, maType = "EMA", wilder = TRUE),
    sma    = TTR::ATR(HLC, n = n, maType = "SMA"),
    ema    = TTR::ATR(HLC, n = n, maType = "EMA")
  )
  atr <- as.numeric(atr_xts[, "atr"])

  hl2 <- (hi + lo) / 2
  upper_basic <- hl2 + multiplier * atr
  lower_basic <- hl2 - multiplier * atr

  upper_final <- rep(NA_real_, N)
  lower_final <- rep(NA_real_, N)
  trend       <- rep(NA_integer_, N)
  st          <- rep(NA_real_, N)

  start <- which(!is.na(atr))[1]
  if (is.na(start) || start >= N) {
    out <- xts::xts(cbind(supertrend = st, trend = trend,
                          upper_band = upper_final,
                          lower_band = lower_final),
                    order.by = zoo::index(HLC))
    return(out)
  }

  upper_final[start] <- upper_basic[start]
  lower_final[start] <- lower_basic[start]
  trend[start]       <- 1L
  st[start]          <- lower_final[start]

  for (i in (start + 1L):N) {
    upper_final[i] <- if (upper_basic[i] < upper_final[i - 1L] ||
                          cl[i - 1L] > upper_final[i - 1L]) {
      upper_basic[i]
    } else {
      upper_final[i - 1L]
    }

    lower_final[i] <- if (lower_basic[i] > lower_final[i - 1L] ||
                          cl[i - 1L] < lower_final[i - 1L]) {
      lower_basic[i]
    } else {
      lower_final[i - 1L]
    }

    trend[i] <- if (cl[i] > upper_final[i - 1L]) {
      1L
    } else if (cl[i] < lower_final[i - 1L]) {
      -1L
    } else {
      trend[i - 1L]
    }

    st[i] <- if (trend[i] == 1L) lower_final[i] else upper_final[i]
  }

  xts::xts(cbind(supertrend = st, trend = trend,
                 upper_band = upper_final, lower_band = lower_final),
           order.by = zoo::index(HLC))
}
```

- [ ] **Step 4: Regenerate documentation and run tests**

```bash
R -q -e 'devtools::document(); devtools::test(filter = "supertrend")'
```

Expected: all 5 tests pass.

If a test fails because `make_mixed_hlc()` doesn't actually produce a flip, inspect `out$trend` and adjust the fixture in `helper-data.R` so the up→down reversal is sharp enough (e.g., narrow the last segment further).

- [ ] **Step 5: Commit**

```bash
git add R/SuperTrend.R tests/testthat/test-supertrend.R man/SuperTrend.Rd NAMESPACE
git commit -m "Implement SuperTrend() with structural and property tests"
```

---

## Task 5: SuperTrend() — input validation tests

**Files:**
- Modify: `tests/testthat/test-supertrend.R`

- [ ] **Step 1: Append validation tests**

Append to `tests/testthat/test-supertrend.R`:

```r
test_that("SuperTrend rejects non-xts input", {
  expect_error(
    SuperTrend(data.frame(High = 1, Low = 1, Close = 1)),
    "HLC must be an xts object"
  )
})

test_that("SuperTrend rejects xts without HLC columns", {
  bad <- xts::xts(matrix(1:4, ncol = 2,
                         dimnames = list(NULL, c("Foo", "Bar"))),
                  order.by = as.Date("2025-01-01") + 0:1)
  expect_error(
    SuperTrend(bad),
    "HLC must contain High, Low, and Close columns"
  )
})

test_that("SuperTrend rejects invalid n", {
  hlc <- make_mixed_hlc()
  expect_error(SuperTrend(hlc, n = 0),    "n must be a positive integer")
  expect_error(SuperTrend(hlc, n = -1),   "n must be a positive integer")
  expect_error(SuperTrend(hlc, n = 1.5),  "n must be a positive integer")
  expect_error(SuperTrend(hlc, n = "10"), "n must be a positive integer")
})

test_that("SuperTrend rejects non-positive multiplier", {
  hlc <- make_mixed_hlc()
  expect_error(SuperTrend(hlc, multiplier = 0),  "multiplier must be positive")
  expect_error(SuperTrend(hlc, multiplier = -1), "multiplier must be positive")
})

test_that("SuperTrend rejects unknown atr_method via match.arg", {
  hlc <- make_mixed_hlc()
  expect_error(SuperTrend(hlc, atr_method = "bogus"))
})
```

- [ ] **Step 2: Run tests**

```bash
R -q -e 'devtools::test(filter = "supertrend")'
```

Expected: all validation tests pass (the implementation already handles these from Task 4).

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-supertrend.R
git commit -m "Add SuperTrend() input validation tests"
```

---

## Task 6: SuperTrend() — atr_method variant tests

**Files:**
- Modify: `tests/testthat/test-supertrend.R`

- [ ] **Step 1: Append atr_method tests**

Append to `tests/testthat/test-supertrend.R`:

```r
test_that("SuperTrend produces different results for each atr_method", {
  hlc <- make_mixed_hlc()
  out_w <- SuperTrend(hlc, atr_method = "wilder")
  out_s <- SuperTrend(hlc, atr_method = "sma")
  out_e <- SuperTrend(hlc, atr_method = "ema")

  # Compare a row past warm-up where ATR differences propagate.
  i <- 20
  expect_false(isTRUE(all.equal(as.numeric(out_w$supertrend[i]),
                                as.numeric(out_s$supertrend[i]))))
  expect_false(isTRUE(all.equal(as.numeric(out_w$supertrend[i]),
                                as.numeric(out_e$supertrend[i]))))
  expect_false(isTRUE(all.equal(as.numeric(out_s$supertrend[i]),
                                as.numeric(out_e$supertrend[i]))))
})

test_that("SuperTrend default atr_method is wilder", {
  hlc <- make_mixed_hlc()
  expect_equal(
    as.numeric(SuperTrend(hlc)$supertrend),
    as.numeric(SuperTrend(hlc, atr_method = "wilder")$supertrend)
  )
})
```

- [ ] **Step 2: Run tests**

```bash
R -q -e 'devtools::test(filter = "supertrend")'
```

Expected: both new tests pass.

If the "different results" assertion fails because SMA and EMA happen to coincide at row 20 with this fixture, change `i` to a different row past warm-up where divergence is clearer.

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-supertrend.R
git commit -m "Add SuperTrend() atr_method variant tests"
```

---

## Task 7: Implement supertrend_signals()

**Files:**
- Create: `tests/testthat/test-signals.R`
- Create: `R/signals.R`

- [ ] **Step 1: Write the failing tests**

Create `tests/testthat/test-signals.R`:

```r
test_that("supertrend_signals returns xts with the expected 0/1 columns", {
  hlc <- make_mixed_hlc()
  st  <- SuperTrend(hlc, n = 10, multiplier = 3)
  sig <- supertrend_signals(st)

  expect_s3_class(sig, "xts")
  expect_equal(colnames(sig),
               c("long_entry", "short_entry", "long_exit", "short_exit"))
  expect_equal(nrow(sig), nrow(st))

  # All values are 0 or 1.
  vals <- unique(as.integer(zoo::coredata(sig)))
  expect_true(all(vals %in% c(0L, 1L)))
})

test_that("supertrend_signals long_entry rows align with short_exit rows", {
  hlc <- make_mixed_hlc()
  sig <- supertrend_signals(SuperTrend(hlc))
  expect_equal(as.integer(sig$long_entry),  as.integer(sig$short_exit))
  expect_equal(as.integer(sig$short_entry), as.integer(sig$long_exit))
})

test_that("supertrend_signals signal counts match number of trend flips", {
  hlc <- make_mixed_hlc()
  st  <- SuperTrend(hlc)
  sig <- supertrend_signals(st)

  trend <- as.numeric(st$trend)
  prev  <- c(NA, head(trend, -1))
  flips_to_up   <- sum(!is.na(prev) & prev == -1 & trend == 1, na.rm = TRUE)
  flips_to_down <- sum(!is.na(prev) & prev == 1  & trend == -1, na.rm = TRUE)

  expect_equal(sum(as.integer(sig$long_entry)),  flips_to_up)
  expect_equal(sum(as.integer(sig$short_entry)), flips_to_down)
})

test_that("supertrend_signals errors on input without trend column", {
  bad <- xts::xts(matrix(1:4, ncol = 2,
                         dimnames = list(NULL, c("foo", "bar"))),
                  order.by = as.Date("2025-01-01") + 0:1)
  expect_error(supertrend_signals(bad),
               "x must be the output of SuperTrend\\(\\)")
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
R -q -e 'devtools::test(filter = "signals")'
```

Expected: tests fail with `could not find function "supertrend_signals"`.

- [ ] **Step 3: Write the implementation**

Create `R/signals.R`:

```r
#' SuperTrend Trading Signals
#'
#' Extracts long/short entry and exit events from the output of
#' \code{\link{SuperTrend}}. A trend flip from \code{-1} to \code{+1}
#' produces a long entry (and a short exit on the same bar). A flip
#' from \code{+1} to \code{-1} produces a short entry (and a long
#' exit). Entry and exit columns are exposed separately because most
#' backtesting frameworks consume them as distinct rules.
#'
#' @param x The \code{xts} object returned by \code{\link{SuperTrend}}.
#'   Must contain a \code{trend} column.
#'
#' @return An \code{xts} aligned to \code{x} with four integer 0/1
#'   columns: \code{long_entry}, \code{short_entry}, \code{long_exit},
#'   \code{short_exit}. Warm-up rows and the first row are 0.
#'
#' @examples
#' data(spy_sample)
#' st  <- SuperTrend(spy_sample)
#' sig <- supertrend_signals(st)
#' tail(sig[rowSums(sig) > 0, ])
#'
#' @export
supertrend_signals <- function(x) {
  if (!xts::is.xts(x) || !"trend" %in% colnames(x)) {
    stop("x must be the output of SuperTrend()")
  }

  trend <- as.numeric(x[, "trend"])
  prev  <- c(NA_real_, utils::head(trend, -1))

  flip_up   <- !is.na(prev) & !is.na(trend) & prev == -1 & trend == 1
  flip_down <- !is.na(prev) & !is.na(trend) & prev == 1  & trend == -1

  long_entry  <- as.integer(flip_up)
  short_entry <- as.integer(flip_down)
  long_exit   <- short_entry
  short_exit  <- long_entry

  xts::xts(cbind(long_entry  = long_entry,
                 short_entry = short_entry,
                 long_exit   = long_exit,
                 short_exit  = short_exit),
           order.by = zoo::index(x))
}
```

- [ ] **Step 4: Regenerate documentation and run tests**

```bash
R -q -e 'devtools::document(); devtools::test(filter = "signals")'
```

Expected: all 4 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/signals.R tests/testthat/test-signals.R man/supertrend_signals.Rd NAMESPACE
git commit -m "Implement supertrend_signals() trend-flip extractor"
```

---

## Task 8: Implement addSuperTrend() and chartSuperTrend()

**Files:**
- Create: `tests/testthat/test-addSuperTrend.R`
- Create: `R/addSuperTrend.R`
- Create: `R/chartSuperTrend.R`

The overlay is fiddly because quantmod's chart object lives in package internals. Rather than reaching into `quantmod:::get.current.chob()`, we layer two `quantmod::addTA` calls — one for the up segments (NA elsewhere) and one for the down segments. `addTA` is exported and handles the chart-object plumbing for us.

- [ ] **Step 1: Write the failing tests**

Create `tests/testthat/test-addSuperTrend.R`:

```r
test_that("chartSuperTrend runs without error on the sample dataset", {
  skip_on_cran()  # rendering is fragile across CRAN's headless devices

  pdf(file = NULL)
  on.exit(dev.off(), add = TRUE)

  data(spy_sample, package = "supertrend")
  expect_no_error(chartSuperTrend(spy_sample, n = 10, multiplier = 3))
})

test_that("addSuperTrend errors when no chart is active", {
  # No active chart -> quantmod's addTA errors; we want some error.
  if (!is.null(dev.list())) {
    for (d in dev.list()) dev.off()
  }
  expect_error(addSuperTrend())
})

test_that("addSuperTrend accepts custom colors and lwd", {
  skip_on_cran()

  pdf(file = NULL)
  on.exit(dev.off(), add = TRUE)

  data(spy_sample, package = "supertrend")
  expect_no_error(
    chartSuperTrend(spy_sample, col = c("blue", "orange"))
  )
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
R -q -e 'devtools::test(filter = "addSuperTrend")'
```

Expected: tests fail with `could not find function "addSuperTrend"` / `chartSuperTrend"`.

- [ ] **Step 3: Implement addSuperTrend()**

Create `R/addSuperTrend.R`:

```r
#' Add SuperTrend Overlay to an Active 'quantmod' Chart
#'
#' Adds a SuperTrend line to the price panel of the currently active
#' \code{\link[quantmod]{chartSeries}} chart. Behaves like
#' \code{\link[quantmod]{addBBands}}: must be called after the chart
#' has been drawn, draws on the price panel by default.
#'
#' The line is drawn in two segments — uptrend bars in \code{col[1]}
#' and downtrend bars in \code{col[2]} — connected by \code{NA} gaps
#' so colors do not bleed across flip bars.
#'
#' @param n,multiplier,atr_method Passed through to
#'   \code{\link{SuperTrend}}.
#' @param col Length-2 character vector of colors:
#'   \code{c(uptrend, downtrend)}. Defaults to TradingView's
#'   green/red.
#' @param lwd Line width.
#' @param on Chart panel to draw on. \code{1} = price panel (the
#'   default and the only sensible choice for SuperTrend).
#'
#' @return Invisibly, the result of the underlying
#'   \code{\link[quantmod]{addTA}} call.
#'
#' @examples
#' \dontrun{
#'   data(spy_sample)
#'   quantmod::chartSeries(spy_sample, theme = "white")
#'   addSuperTrend()
#' }
#'
#' @export
addSuperTrend <- function(n = 10, multiplier = 3,
                          atr_method = c("wilder", "sma", "ema"),
                          col = c("#26a69a", "#ef5350"),
                          lwd = 2, on = 1) {
  atr_method <- match.arg(atr_method)
  if (!is.character(col) || length(col) != 2L) {
    stop("col must be a length-2 character vector: c(uptrend, downtrend)")
  }

  lchob <- quantmod:::get.current.chob()
  if (is.null(lchob)) {
    stop("addSuperTrend() must be called after an active chartSeries() chart")
  }
  x <- lchob@xdata

  st <- SuperTrend(x, n = n, multiplier = multiplier,
                   atr_method = atr_method)

  st_up <- st[, "supertrend"]
  st_up[as.numeric(st[, "trend"]) != 1] <- NA
  colnames(st_up) <- "SuperTrend (up)"

  st_dn <- st[, "supertrend"]
  st_dn[as.numeric(st[, "trend"]) != -1] <- NA
  colnames(st_dn) <- "SuperTrend (down)"

  quantmod::addTA(st_up, on = on, col = col[1], lwd = lwd)
  invisible(quantmod::addTA(st_dn, on = on, col = col[2], lwd = lwd))
}
```

Note the `quantmod:::` (triple-colon) call. This is required to read the active chart object and is the same pattern `quantmod::addBBands` uses internally. It will trigger an `R CMD check` note about non-exported function use; we'll suppress it with a `globalVariables` declaration if needed in Task 13.

- [ ] **Step 4: Implement chartSuperTrend()**

Create `R/chartSuperTrend.R`:

```r
#' Chart a Series with SuperTrend Overlay
#'
#' Convenience wrapper that draws a \code{\link[quantmod]{chartSeries}}
#' price chart and overlays the SuperTrend line in one call.
#'
#' @param x An \code{xts} OHLC series.
#' @param ... Additional arguments passed to
#'   \code{\link[quantmod]{chartSeries}} (e.g., \code{theme}, \code{type},
#'   \code{subset}, \code{TA}).
#' @param name Chart title. Defaults to the deparsed input expression.
#' @param n,multiplier,atr_method,col Passed through to
#'   \code{\link{addSuperTrend}}.
#'
#' @return Invisibly \code{NULL}; called for the side effect of drawing.
#'
#' @examples
#' \dontrun{
#'   data(spy_sample)
#'   chartSuperTrend(spy_sample, theme = "white")
#' }
#'
#' @export
chartSuperTrend <- function(x, ..., name = deparse(substitute(x)),
                            n = 10, multiplier = 3,
                            atr_method = c("wilder", "sma", "ema"),
                            col = c("#26a69a", "#ef5350")) {
  atr_method <- match.arg(atr_method)
  quantmod::chartSeries(x, name = name, ...)
  addSuperTrend(n = n, multiplier = multiplier,
                atr_method = atr_method, col = col)
  invisible(NULL)
}
```

- [ ] **Step 5: Regenerate documentation and run tests**

```bash
R -q -e 'devtools::document(); devtools::test(filter = "addSuperTrend")'
```

Expected: all 3 tests pass.

If `chartSuperTrend()` errors during the test, the most likely cause is the `pdf(file = NULL)` device not properly registering as the active device for `quantmod::chartSeries`. Workaround: `pdf(tempfile(fileext = ".pdf"))` — and ensure `unlink()` in the `on.exit`.

- [ ] **Step 6: Commit**

```bash
git add R/addSuperTrend.R R/chartSuperTrend.R tests/testthat/test-addSuperTrend.R man/addSuperTrend.Rd man/chartSuperTrend.Rd NAMESPACE
git commit -m "Implement addSuperTrend() and chartSuperTrend() overlay"
```

---

## Task 9: Write the vignette

**Files:**
- Create: `vignettes/supertrend.Rmd`

- [ ] **Step 1: Write the vignette**

Create `vignettes/supertrend.Rmd`:

````markdown
---
title: "Using the supertrend Package"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Using the supertrend Package}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment  = "#>",
  fig.width  = 7,
  fig.height = 4
)
```

## Overview

`supertrend` adds the SuperTrend technical indicator and its trading
signals to the `quantmod` workflow. The calculation matches what users
see on TradingView: ATR-based bands with a configurable multiplier,
trailing in the direction of the trend until price closes through them.

## Computing the indicator

```{r}
library(supertrend)
data(spy_sample)

st <- SuperTrend(spy_sample, n = 10, multiplier = 3)
head(st, 15)
```

The output xts has four columns:

- `supertrend` — the indicator line
- `trend` — `+1` (uptrend) or `-1` (downtrend)
- `upper_band`, `lower_band` — the trailing bands

## Plotting

```{r}
chartSuperTrend(spy_sample, theme = "white",
                n = 10, multiplier = 3)
```

The line is green during uptrends and red during downtrends. To layer it
onto an existing chart instead, use `addSuperTrend()`:

```{r}
quantmod::chartSeries(spy_sample, theme = "white", name = "SPY (synthetic)")
addSuperTrend(n = 10, multiplier = 3)
```

## Extracting trading signals

```{r}
sig <- supertrend_signals(st)
sig[rowSums(sig) > 0, ]
```

`long_entry`, `short_entry`, `long_exit`, and `short_exit` are 0/1 flags
on flip bars. By construction, `long_entry == short_exit` and
`short_entry == long_exit` — they are exposed separately so they can be
fed to backtesting frameworks (such as `quantstrat`) as independent
rules.

## A trivial signal-following backtest sketch

```{r}
position <- cumsum(as.numeric(sig$long_entry) - as.numeric(sig$short_entry))
position <- ifelse(position > 0, 1, ifelse(position < 0, -1, 0))
returns  <- diff(log(as.numeric(spy_sample$Close)))
strategy_returns <- head(position, -1) * tail(returns, -1)
sum(strategy_returns, na.rm = TRUE)
```

This is a sketch — production backtesting needs entry/exit timing
assumptions, slippage, and position sizing. See `quantstrat` or
`PerformanceAnalytics`.

## Tuning ATR smoothing

The default ATR smoothing is Wilder's (matches TradingView). For SMA or
plain EMA smoothing:

```{r}
SuperTrend(spy_sample, atr_method = "sma")  |> head(15)
SuperTrend(spy_sample, atr_method = "ema")  |> head(15)
```
````

- [ ] **Step 2: Build the vignette to verify it knits**

```bash
R -q -e 'devtools::build_vignettes()'
```

Expected: produces `doc/supertrend.html` (or similar) without errors.

- [ ] **Step 3: Commit**

```bash
git add vignettes/
git commit -m "Add supertrend vignette"
```

---

## Task 10: Write the README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write the README**

Create `README.md`:

````markdown
# supertrend

SuperTrend technical indicator and signals for [quantmod](https://www.quantmod.com).

The SuperTrend indicator is an ATR-based trailing band that flips trend
direction when price closes through the opposing band. This package
provides a calculation that matches TradingView's `ta.supertrend`,
extracts long/short entry and exit signals on trend flips, and adds a
`quantmod`-idiomatic chart overlay.

## Installation

```r
# install.packages("remotes")
remotes::install_github("your-account/supertrend")
```

## Quickstart

```r
library(supertrend)
library(quantmod)

# Use the built-in synthetic dataset (or fetch with getSymbols)
data(spy_sample)

# 1. Compute the indicator
st <- SuperTrend(spy_sample, n = 10, multiplier = 3)
head(st, 15)

# 2. Plot it
chartSuperTrend(spy_sample, theme = "white")

# 3. Extract signals
sig <- supertrend_signals(st)
sig[rowSums(sig) > 0, ]
```

## API

| Function | Purpose |
|---|---|
| `SuperTrend(HLC, n, multiplier, atr_method)` | Compute the indicator. Returns an xts with `supertrend`, `trend`, `upper_band`, `lower_band`. |
| `supertrend_signals(st)` | Extract long/short entry/exit events on trend flips. |
| `addSuperTrend(...)` | quantmod overlay — call after `chartSeries()`. |
| `chartSuperTrend(x, ...)` | One-liner: chart and overlay in one call. |

See `vignette("supertrend")` for a full walkthrough.

## License

MIT
````

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "Add README with quickstart"
```

---

## Task 11: R CMD check --as-cran cleanup

**Files:**
- Modify (as needed): `DESCRIPTION`, `R/*.R`, `.Rbuildignore`

- [ ] **Step 1: Run R CMD check --as-cran**

```bash
R -q -e 'devtools::check(args = "--as-cran")'
```

Expected output to scan:

- 0 errors
- 0 warnings
- Acceptable notes: "New submission", "Days since last update".
- Notes that require fixing: any "no visible binding for global variable", "Undefined global functions or variables", "non-exported call to quantmod:::get.current.chob".

- [ ] **Step 2: Fix the `quantmod:::` note**

If `R CMD check` complains about `quantmod:::get.current.chob`, document the necessity and add to `R/package.R`:

```r
# Add at the bottom of R/package.R
utils::globalVariables(c("get.current.chob"))
```

If the note persists, the cleanest fix is to wrap the call in `getFromNamespace`:

```r
# In R/addSuperTrend.R, replace:
#   lchob <- quantmod:::get.current.chob()
# With:
get_chob <- utils::getFromNamespace("get.current.chob", "quantmod")
lchob    <- get_chob()
```

`getFromNamespace` is the CRAN-blessed way to call non-exported functions.

- [ ] **Step 3: Fix any "no visible binding" notes**

For roxygen-doc references like `\code{\link[quantmod]{addBBands}}`, ensure the linked function exists in the imported version of quantmod.

For unbound names inside functions (e.g., `dev.off`, `pdf`), add `@importFrom grDevices dev.off pdf` to `R/package.R` if needed.

- [ ] **Step 4: Re-run check until clean**

```bash
R -q -e 'devtools::check(args = "--as-cran")'
```

Iterate until: 0 errors, 0 warnings, 0 notes (or only the unavoidable "new submission" note).

- [ ] **Step 5: Commit any fixes**

```bash
git add -A
git commit -m "Fix R CMD check --as-cran notes"
```

---

## Task 12: Final smoke test

**Files:** none modified

- [ ] **Step 1: Install and load the package fresh**

```bash
R -q -e 'devtools::install(); library(supertrend); ?SuperTrend; ?supertrend_signals; ?addSuperTrend; ?chartSuperTrend'
```

Expected: package installs without errors, all four help pages render.

- [ ] **Step 2: Run the full test suite one more time**

```bash
R -q -e 'devtools::test()'
```

Expected: all tests pass.

- [ ] **Step 3: Render an actual chart to verify visually**

```bash
R -q -e 'library(supertrend); data(spy_sample); png("/tmp/supertrend_smoke.png", width = 1200, height = 600); chartSuperTrend(spy_sample, theme = "white"); dev.off()'
```

Then open `/tmp/supertrend_smoke.png`. Verify by eye:

- The candle chart renders normally.
- A SuperTrend line is overlaid, switching between green (uptrend) and red (downtrend) at flip bars.
- Colors do not bleed across flips (no diagonal line connecting a green segment to a red one).

- [ ] **Step 4: Tag the release**

```bash
git tag -a v0.1.0 -m "supertrend 0.1.0 — initial release"
```

Implementation complete.

---

## Self-review

**Spec coverage check:**

| Spec section | Task |
|---|---|
| Public API: `SuperTrend()` | Tasks 4, 5, 6 |
| Public API: `supertrend_signals()` | Task 7 |
| Public API: `addSuperTrend()` | Task 8 |
| Public API: `chartSuperTrend()` | Task 8 |
| Dependencies (Imports/Suggests, R ≥ 3.6.0) | Task 1 (DESCRIPTION) |
| Repository layout | Tasks 1-10 (all create files at the spec's paths) |
| Core algorithm pseudocode → R | Task 4 (`R/SuperTrend.R`) |
| ATR maType pass-through (wilder/sma/ema) | Task 4 (impl) + Task 6 (tests) |
| Input validation errors | Task 4 (impl) + Task 5 (tests) |
| Signal extraction logic | Task 7 |
| Bicolor line via two `addTA` calls with NA masking | Task 8 (`R/addSuperTrend.R`) |
| Test fixture: synthetic 30-bar OHLC, no network | Task 3 |
| testthat tests covering structural, property, validation, numeric variation | Tasks 4, 5, 6, 7, 8 |
| Roxygen2 docs with @return and @examples | Tasks 4, 7, 8 |
| Vignette | Task 9 |
| README | Task 10 |
| Built-in sample dataset | Task 2 |
| `R CMD check --as-cran` clean | Task 11 |

All spec sections mapped to tasks.

**Placeholder scan:** No "TODO", "TBD", "implement later", or "similar to Task N" references. Every code step contains the actual code.

**Type/name consistency:**

- `SuperTrend()` returns columns `supertrend`, `trend`, `upper_band`, `lower_band` — used consistently in Tasks 4, 7, 8.
- `supertrend_signals()` returns columns `long_entry`, `short_entry`, `long_exit`, `short_exit` — used consistently in Tasks 7 and 9 (vignette).
- `atr_method` argument values `"wilder"` / `"sma"` / `"ema"` — consistent across Tasks 4, 6, 8.
- `col` argument is always a length-2 character vector `c(uptrend, downtrend)` — Tasks 8 and 9.
- `addSuperTrend()` parameter list (`n`, `multiplier`, `atr_method`, `col`, `lwd`, `on`) matches the spec.

No inconsistencies.
